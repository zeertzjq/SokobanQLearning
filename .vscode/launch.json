{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/CLI/CLI",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/CLI",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb",
            "preLaunchTask": "debug"
        }
    ]
}
